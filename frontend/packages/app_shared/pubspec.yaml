name: app_shared
description: Shared screens and services for Guitar AI Tutor apps
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  network:
    path: ../network
  shared_ui:
    path: ../shared_ui
  shared_preferences: ^2.3.3
  record: ^5.2.0
  cross_file: ^0.3.4+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
