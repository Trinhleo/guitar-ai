name: app_mobile
description: Guitar AI Tutor mobile app (iOS + Android)
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  network:
    path: ../../packages/network
  shared_ui:
    path: ../../packages/shared_ui
  shared_preferences: ^2.3.3
  record: ^5.2.0
  cross_file: ^0.3.4+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
