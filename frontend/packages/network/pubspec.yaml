name: network
description: API client for the Guitar AI Go backend
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.2
  web_socket_channel: ^3.0.1

dev_dependencies:
  test: ^1.25.8
